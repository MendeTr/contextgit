{
  "name": "contextgit",
  "private": false,
  "version": "0.0.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "bin": {
    "contextgit": "./packages/cli/bin/run.js"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "sqlite-vec", "esbuild"]
  }
}
